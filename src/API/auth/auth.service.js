// import toast from 'toastify';
// import axios from 'axios';

class AuthService {
  apiPath = "";
  async login(user) {
    console.log(user);
    // try {
    //   const response = await axios.get('sdf');
    //   return response.data;
    // } catch (e) {
    //   toast.error(e.message || 'Something went wrong');
    //   console.error(e);
    // }
  }
}

export default AuthService;
